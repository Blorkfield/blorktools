name: Version and Publish

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  version-and-publish:
    runs-on: ubuntu-latest
    # Skip if this is a version bump commit (avoid infinite loop)
    if: "!contains(github.event.head_commit.message, 'chore(release):')"

    steps:
      - name: Generate bot token
        id: bot-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ steps.bot-token.outputs.token }}

      - name: Setup Git
        run: |
          git config user.name "blork-bot[bot]"
          git config user.email "blork-bot[bot]@users.noreply.github.com"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Version packages
        run: pnpm version-packages --yes
        env:
          GH_TOKEN: ${{ steps.bot-token.outputs.token }}

      - name: Push version commit and tags
        run: git push --follow-tags

      - name: Build
        run: pnpm build

      - name: Publish packages
        run: pnpm publish-packages --yes
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
